server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/revature_hydra/hydra-configuration/tree/feature-gambit-config-dev
          username: revdum
          password: p4ssw0rd
  application:
    name: gambit-config-serve
