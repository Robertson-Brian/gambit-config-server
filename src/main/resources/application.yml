server:
  port: 8888

spring:
  application:
    name:
      gambit-config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_HOME}
          username: revdum
          password: p4ssw0rd
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: gambit
    bootstrap-servers:
    - ${KAFKA_URL}
